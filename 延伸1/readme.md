# 說明延伸1的內容
* 我們在實作的時候發現我們每次在決定分策略時要用高斯還是要用cubic都需要自己手動去做，我們就在想有沒有甚麼可以比較自動化的方法來做
* 因此我們開始實作，簡單來說，我們用了一個數學公式，也就是 Rippa （其中是模型計算出的權重，是核矩陣求逆後的對角線數值）。
這個公式可以在不需要重新訓練模型的情況下，一瞬間算出「Cubic」和「Gaussian」這兩種核心對每個點的預測誤差 ，然後讓程式自動挑選預測誤差最小（預測最準）的那一個核心來使用。
